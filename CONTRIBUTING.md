# Contributing

Create a focused branch, keep changes small, and run `cargo fmt --check` and `cargo check` before requesting review. Add tests when behavior is introduced. Do not commit credentials, hostnames, SSH paths, cluster configuration, or large generated renders.

